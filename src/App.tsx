import ReactDOM from 'react-dom';

const HelloWorld = () => {
    return (
        <h1>
            monaco test1
        </h1>
    );
}

ReactDOM.render(<HelloWorld />, document.getElementById("root"));